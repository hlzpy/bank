export enum UserType {
  NormalUser = "普通用户",
  Admin = "管理员",
  BankInstitution = "银行机构",
}

export enum Keys {
  // 当前登录用户
  CurrentLoginUser = "CurrentLoginUser",
  // 所有已注册的用户
  AllRegisteredUsers = "AllRegisteredUsers",
  // 转账
  TransferMoney = "TransferMoney",
}
